import { Routes } from '@angular/router';
import { AssignedrequirementComponent } from './assignedrequirement/assignedrequirement.component';

export const routes: Routes = [

  { path: '', component: AssignedrequirementComponent },

]